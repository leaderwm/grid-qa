# 能力收益矩阵（开关对照评测结论）

> **日期:** 2026-09-03 ｜ **数据来源:** `reports/eval_matrix_20260903_merged.md`（三次运行合并：020625 / 065135 / 084816）
> **定位:** 方向 3（已建能力灰度验证）的产出文档。verdict 皆为**建议**，开关变更需人工评审后改 `.env`（关=现状语义见 `config.py` 注释）。
> **⚠️ 环境事实:** 评测时 DASHSCOPE 欠费（chat 400）+ DeepSeek 余额不足（402），LLM 链实际走 qwen(败)→deepseek(败)→doubao 兜底。**LLM 依赖变体的结论需在 LLM 健康环境复测后才可定案。**

## 检索维（golden 109 条，topk=5）

| 变体 | 开关 | Δ召回 | ΔMRR | ΔnDCG | 建议 | 备注 |
|---|---|---|---|---|---|---|
| baseline | — | — | — | — | — | recall 67.9%（旧 32 条精选集 93.8%；扩充集含多跳/对比/换皮问法） |
| rrf_route_aware | `RRF_ROUTE_AWARE_ENABLE` | +0.0pp | +0.4pp | +2.1pp | 维持关闭 | nDCG 有微增但 recall 无动 |
| crag_neighbor | `CRAG_NEIGHBOR_EXPAND_ENABLE` | +0.0pp | +0.0pp | +0.0pp | 维持关闭 | |
| raptor | `RAPTOR_ENABLE` | +0.0pp | +0.0pp | +0.0pp | 维持关闭 | |
| hyde | `HYDE_ENABLE` | +0.0pp | +0.0pp | +0.0pp | 维持关闭 | ⚠️ 与 baseline 完全同值——疑似降级链下 HyDE 生成为空退化原查询，结论不可靠 |
| multi_query | `MULTI_QUERY_ENABLE` | **-7.3pp** | -11.9pp | -10.2pp | **建议回收** | 本语料上多查询分解稳定倒退 |
| self_rag | `SELF_RAG_ENABLE` | +0.0pp | +0.0pp | +0.0pp | 维持关闭 | ⚠️ need_retrieve 的 LLM 调用失败→退化 baseline 行为，结论不可靠 |

## 生成维（每变体 4 条小样，支撑率=LLM-judge）

| 变体 | 开关 | Δ支撑率 | Δ幻觉率 | Δ时延 | 建议 | 备注 |
|---|---|---|---|---|---|---|
| baseline | — | — | — | — | — | 时延 137.8s 系欠费 fallback 链所致，非正常水平 |
| crag_v3 | `CRAG_V3_ENABLE` | +16.7pp | -16.7pp | +28s | 常开候选 | |
| crag_perdoc | `CRAG_PERDOC_ENABLE` | +16.7pp | -16.7pp | **-50s** | 常开候选 | v2 逐条评估反而更快（v1 路径多一次改写重检索？需复测确认） |
| citation_verifier | `CITATION_VERIFIER_ENABLE` | +16.7pp | -16.7pp | -49s | 常开候选 | |
| query_rewrite | `QUERY_REWRITE_ENABLE` | +16.7pp | -16.7pp | +1s | 常开候选 | |
| citation_nli | `CITATION_NLI_ENABLE` | -50.0pp | +50.0pp | -38s | 回收？ | 0% 支撑疑似小样+judge 判定口径问题，复测前不下结论 |
| debate | `DEBATE_ON_LOW_CONFIDENCE_ENABLE` | -16.7pp | +16.7pp | -1s | 回收？ | |
| sufficiency_gate | `SUFFICIENCY_GATE_ENABLE` | -50.0pp | +50.0pp | -41s | 回收？ | 同 citation_nli，疑似口径问题 |
| semantic_cache | `SEMANTIC_CACHE_ENABLE` | -50.0pp | +50.0pp | -49s | 首跑不命中 | 收益看二次运行时延列，需复测 |

## 复测条件与已知口径限制

1. **LLM 充值后复测**：`python scripts/eval_matrix.py --limit 6`（qwen/deepseek 任一恢复即可），重点复测 hyde / self_rag / citation_nli / sufficiency_gate / semantic_cache / debate 六个存疑变体。
2. **semantic_cache 需跑两遍**（首跑必不命中）。
3. 生成维样本量 ≥6 再谈 verdict（4 条时 1 条翻转即 ±25pp）。
4. 首跑曾暴露并修复的缺陷（勿回退）：
   - `retrieval_eval_service` 打分口径（精确相等→docName 子串+relevant_docs 分级）；
   - `_login_probe` 就绪判定（空 body 422 + Windows 系统代理劫持 127.0.0.1）；
   - `eval_generation` 来源文本字段（`chunk` 而非 `text`）、judge 脏样本跳过；
   - 矩阵探针强制 `EVAL_EMIT/EVAL_TO_TUNE=false`（防飞轮在探针进程内重扫）。
